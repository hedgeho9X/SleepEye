# SleepEye Agent 指南

## 沟通偏好

- 默认使用简体中文回复。
- 引用本地源码文件时，使用 Zed 可点击格式：file:///absolute/path#L<line>。
- 文件引用不要包在反引号里。
- 如果要执行关键命令，先用一句话说明要做什么和为什么。

## 项目定位

SleepEye 是一个番茄钟形式的定时休息护眼 macOS 本地小工具。产品方向是轻量、安静、可靠，像系统工具一样常驻，不做复杂效率平台。

核心目标：

- 到点提醒用户休息眼睛、看远处、起身或眨眼。
- 以菜单栏和顶部胶囊/刘海区浮层作为主要展示形式。
- 本地运行，不要求登录，不依赖后端，不上传用户数据。
- 功能保持克制，先把计时、提醒、暂停、跳过和基础设置做稳。

## 展示参考

展示形式参考 CodeIsland：顶部轻量状态面板、空闲收起、状态变化时展开、菜单栏入口兜底。

借鉴重点：

- 顶部胶囊/刘海区状态面板。
- 菜单栏常驻入口。
- 多显示器和非刘海屏的降级处理。
- 不打断用户工作的提醒节奏。

不要照搬：

- 复杂 hook、IPC、Unix socket、agent 事件管线。
- 多工具适配体系。
- 过重的角色动画、声音和远程能力。

## 推荐技术方向

- 首选 SwiftUI + 少量 AppKit。
- SwiftUI 用于菜单栏面板、设置页、状态展示等界面。
- AppKit 用于 `NSPanel` 顶部浮层、窗口层级、非激活面板、多显示器定位等 macOS 细节。
- 本地设置优先用 `UserDefaults` / `AppStorage`。
- 本地统计 MVP 可用 JSON 或轻量存储，除非确有需要再引入 SQLite。
- 通知使用 macOS `UserNotifications`。
- 开机启动可在 MVP 后期再接 `ServiceManagement`。

不推荐一开始使用 Electron、Tauri、Flutter 或 React Native。这个项目的核心是 macOS 系统级小工具体验，原生路线更稳、更轻。

## MVP 范围

优先完成：

- 25/5 番茄钟。
- 50/10 长专注。
- 20-20-20 护眼提醒。
- 开始、暂停、跳过休息、立即休息。
- 菜单栏状态与快捷操作。
- 顶部胶囊提醒。
- 系统通知。
- 基础设置：专注时长、休息时长、自动下一轮、提醒强度。

暂缓：

- 账号系统。
- 云同步。
- AI 推荐作息。
- 强制锁屏或阻止键鼠。
- 团队协作。
- 复杂统计报表。

## 体验原则

- 默认柔性提醒，不做强制锁死。
- 用户永远可以延后、跳过或暂停。
- 浮层不应长期占据大面积屏幕。
- 全屏应用和多显示器场景下，菜单栏/系统通知必须作为兜底。
- 计时逻辑使用目标结束时间计算剩余时间，避免系统睡眠或唤醒导致倒计时漂移。

## 代码组织建议

建议按职责拆分：

- `TimerCore`：专注、休息、暂停、空闲状态机。
- `AppState`：连接计时、设置、通知和 UI。
- `OverlayWindowController`：顶部浮层创建、定位、显示和隐藏。
- `MenuBarController`：菜单栏图标、状态展示和快捷操作。
- `SettingsStore`：配置读写。
- `NotificationScheduler`：通知权限和提醒发送。
- `HistoryStore`：今日番茄数、休息次数等轻量统计。

## 代码规范

- 优先写简单、直接、可读的 Swift 代码，不为了“架构感”提前抽象。
- SwiftUI 视图只负责展示和用户操作转发，不直接承载计时、通知、存储、窗口定位等业务逻辑。
- 业务状态集中放在 `TimerCore` / `AppState` 这一类可测试对象中，避免多个 View 各自维护重复状态。
- AppKit 代码只包在必要的 controller / adapter 内，例如 `OverlayWindowController`，不要让 AppKit 细节散落到 SwiftUI 视图里。
- 命名使用清晰的英文，类型名用大驼峰，变量、函数和属性用小驼峰；不要使用含糊缩写，例如 `mgr`、`tmp`、`data1`。
- 单个文件只放高度相关的类型；如果一个文件同时出现计时、通知、设置、窗口控制等多个职责，应主动拆分。
- 函数尽量短小，优先表达一个明确动作；超过 40 行时先考虑拆出私有方法。
- 避免魔法数字。番茄钟时长、提醒阈值、动画时长、窗口尺寸等应放入常量或配置结构。
- 时间计算以“开始时间 + 目标结束时间”为准，不依赖每秒递减作为真实状态，避免系统睡眠、卡顿或唤醒导致计时漂移。
- 涉及 UI 状态变更时，明确保证在主线程执行；异步代码不要悄悄修改 SwiftUI 状态。
- 任何系统权限、通知、开机启动、窗口层级相关逻辑，都要能优雅失败，并给 UI 留出可恢复状态。
- 不引入大型第三方依赖，除非它明显降低复杂度并且适合本地 macOS 小工具。
- 新增代码时同步考虑可测试性。`TimerCore`、配置读写、状态切换规则应优先写成不依赖真实 UI 的纯逻辑。

## 中文注释规范

- 代码注释统一使用中文，面向后续维护者解释“为什么这样做”和“边界条件是什么”，不要重复代码表面含义。
- 公开类型、核心状态、核心方法必须写 `///` 文档注释，说明职责、输入输出、状态变化和重要副作用。
- 涉及 macOS 系统行为的代码必须写注释，例如 `NSPanel` 层级、非激活窗口、多显示器定位、通知权限、开机启动、全屏空间兼容。
- 涉及计时准确性的逻辑必须写注释，说明为什么使用目标结束时间、如何处理暂停、恢复、睡眠唤醒和跳过休息。
- 涉及用户体验取舍的逻辑必须写注释，例如为什么默认柔性提醒、为什么允许延后/跳过、为什么全屏时降级为系统通知。
- 复杂分支前用一小段注释说明判断顺序；不要在每一行后面追加碎片化尾注。
- 临时 workaround 必须写明原因、触发条件和后续移除条件，格式建议为：`// FIXME: 原因；移除条件：...`。
- 禁止留下无意义注释，例如“设置变量”“调用方法”“返回结果”。
- 如果注释和代码不一致，以代码为准并立即更新注释；不要让旧注释误导后续实现。

示例：

```swift
/// 管理专注与休息的状态切换。
///
/// 计时不依赖每秒递减，而是记录当前阶段的目标结束时间。
/// 这样系统睡眠、应用短暂卡顿或菜单栏刷新延迟时，恢复后仍能得到正确剩余时间。
final class TimerCore {
    // 休息提醒允许延后，是为了避免在演示、会议或不可中断任务中强行打断用户。
    func deferBreak(by interval: TimeInterval) {
        // ...
    }
}
```

## 自动提交规范

- 每完成一个独立 feature，默认自动创建一次 git commit，除非用户明确说“先不要提交”或“只改不提交”。
- commit 前先查看 `git status --short`，只暂存本 feature 相关文件，不把无关修改、用户已有改动、临时文件一起提交。
- 如果仓库已有测试、格式化或构建命令，commit 前应尽量运行与本 feature 相关的最小验证；无法运行时在最终回复里说明原因。
- 如果验证失败，不自动提交；先修复问题。如果失败与本次 feature 无关，要在回复里说明并等待用户决定。
- commit 信息使用中文说明，建议保留 Conventional Commit 前缀，例如 `feat: 添加顶部休息提醒`、`fix: 修复暂停后计时漂移`、`docs: 补充协作规范`。
- 一个 feature 对应一个 commit；不要把多个独立功能揉进同一个提交。
- 如果实现过程中发现需要拆分 feature，应拆成多个小提交，每个提交保持可理解、可回退。
- 提交后在回复里给出 commit hash 和本次验证结果。

## 当前规划资料

- 本地规划草稿：file:///Users/jerry/Code/project/SleepEye/sleepeye_lark_plan.xml#L1
- 飞书规划文档：https://my.feishu.cn/docx/GyvNdb2B4o5D5exIuupcgYgrnoe
